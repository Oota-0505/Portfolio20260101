# 個人開発メモ：Work Home Dashboard（通知管理アプリ）

> ポートフォリオ「個人開発（Personal）」セクション用の説明文ドラフト。
> 明日この内容をもとにカード＋詳細を作成する。スクショは images/ に保存して使う。

## カード用ショート説明（Worksカードのdesc候補）
仕事で必要な通知・予定・写真を1つの画面にまとめた、自分専用のローカルダッシュボード。Gmail・Chatwork・GitHub・Slackの通知を集約し、Notionカレンダーとフォトライブラリーを並べて、外部ディスプレイに常時表示できる仕事用ホーム画面として制作。

## タグ候補
Node.js / JavaScript / Gmail API / Chatwork API / GitHub API / AppleScript / 個人開発 / ローカル実行

---

# Work Home Dashboard
自分の仕事用ホーム画面を作るために開発した、ローカル実行型のデスクトップダッシュボードです。
右側にはNotionカレンダー、左上には仕事通知、左下には写真・動画のフォトライブラリーを配置し、外部ディスプレイに常時表示して使うことを想定しています。
単なる通知アプリではなく、仕事中に常に見ておきたい情報と、少し気分が上がる要素を1つの画面にまとめた「自分専用の仕事ホーム画面」です。

## 背景
仕事中に確認すべき情報が複数の場所に分散していました（Gmail / Chatwork / GitHub / Slack / Notionカレンダー）。それぞれを個別に開いて確認する必要があり、通知の見落としや確認漏れが起きやすい状態でした。
特にGmailは、すべてのメールではなく、特定の相手から来たメールだけを見逃したくないという要件がありました。ChatworkやGitHubも、仕事上すぐ確認すべき通知だけを把握したいという目的がありました。
また、Macの標準ウィジェットでは写真を表示することはできますが、基本的にデスクトップの背面に配置されるため、作業中の画面構成には組み込みにくい問題がありました。通知パネルやカレンダーと並べて、同じ画面上で写真や動画を切り替えて表示するには、自分用に作る必要がありました。
そこで、通知管理・カレンダー・フォトライブラリーを組み合わせた、自分専用のホームダッシュボードを作成しました。

## アプリの概要
Work Home Dashboardは、以下の3つを組み合わせた仕事用ダッシュボードです。

1. 通知管理パネル
2. Notionカレンダー表示
3. 写真・動画フォトライブラリー

メインとなるのは通知管理パネルです。Gmail、Chatwork、GitHub、Slackの通知を1つの画面にまとめ、確認すべきものがある時だけ表示します。
通知がない時はシンプルにアイコンだけを表示し、余計な情報を出さないようにしています。通知が来た時だけ内容を表示することで、常時表示していても邪魔にならないUIにしました。

## 通知管理機能
### Gmail
Gmail APIを使い、指定した送信者から届いた未読メールだけを取得します。すべてのメールを通知対象にするのではなく、仕事上見逃したくない相手だけを登録する設計にしています。通知には件名や本文の一部を表示し、どのメールを確認すべきかすぐ分かるようにしています。
また、別のGmailアカウントに届くメールも、Gmailのフィルタ転送を使って通知対象にできるようにしています。

### Chatwork
Chatwork APIを使い、未読メッセージや自分宛ての通知を取得します。単なる未読数ではなく、可能な範囲で送信者名や本文の一部も表示するようにしています。Chatworkを開く前に、誰からどんな内容が来ているかを軽く確認できます。

### GitHub
GitHub Notifications APIを使い、レビュー依頼やコメント通知を取得します。GitHubの通知は開発作業上の優先度が高いため、通知パネル内で確認できるようにしています。PRレビューやコメントの見落としを減らすことを目的にしています。

### Slack
SlackはBot追加やAPI連携を避け、Chromeで開いているSlackタブの未読表示を監視する方式にしています。Slack APIやBotを使うと、チャンネル側にBot追加の表示が出たり、相手側に影響が出る可能性があります。そのため、仕事用Chromeで開いているSlack画面をローカルで監視し、新着がある場合だけ通知として表示する実装にしました。

## Photo Library
左下には写真・動画をランダムに表示するフォトライブラリーを配置しています。Mac標準の写真ウィジェットでは、画像がデスクトップの背面に表示されるため、通知パネルやカレンダーと並べて常時表示する用途には合いませんでした。
そこで、指定フォルダ内の写真や動画を読み込み、ブラウザ上でスライド表示する小さなフォトフレームを実装しました。通知がない時間でも画面が無機質にならず、外部ディスプレイに置きっぱなしにしたくなる作業環境を作るための機能です。

## UI設計
画面全体は、外部ディスプレイに常時表示することを前提に設計しています。

- 左上: 通知パネル
- 左下: フォトライブラリー
- 右側: Notionカレンダー

通知パネルは、情報量をできるだけ減らしたミニマルなUIにしています。通知がない時は、各サービスのアイコンだけを表示します。通知がある時だけカード内に内容を表示し、必要な時だけ視線が向くようにしています。
目的は、アプリ内で全部を完結させることではなく、「今どのサービスを見に行くべきか」を一瞬で判断できることです。

## ローカル実行
このアプリは自分のPCだけで動作します。外部サーバーやデータベースは使わず、Node.jsのローカルサーバーで画面表示とAPI確認を行います。
APIキーや認証情報は `.env` に保存し、Gmail OAuthトークンや通知状態はローカルの `data/` に保存します。

## 使用技術
- Node.js
- JavaScript
- HTML / CSS
- Gmail API
- Chatwork API
- GitHub Notifications API
- Google Chrome / AppleScript
- ローカルファイル管理

## 工夫した点
- Gmailは特定送信者のみを通知対象にした
- 別Gmailアカウントの特定メールも転送で拾えるようにした
- Chatworkは送信者名と本文プレビューを表示するよう改善した
- GitHub通知を仕事上重要な通知として統合した
- SlackはBot追加せず、Chromeタブ監視で相手側に影響を出さない方式にした
- 通知がない時は何も表示しないミニマルなUIにした
- 写真・動画フォルダを読み込むフォトライブラリーを追加した
- 外部ディスプレイに常時表示しやすい構成にした

## このアプリで解決したこと
このアプリによって、仕事中に複数サービスを何度も開いて確認する必要が減りました。通知が来ていなければ何も考えなくてよく、通知が来た時だけ対象サービスを見に行けます。また、Notionカレンダーと写真ライブラリーを同じ画面に並べることで、仕事用のホーム画面として使いやすくなりました。

## 今後の改善案
- Slack通知の検知精度向上
- Gmail複数アカウントの直接対応
- 通知の重要度分類
- 通知履歴の管理
- 画面レイアウトのプリセット化
- 外部ディスプレイ向けの自動配置

## まとめ
Work Home Dashboardは、仕事で必要な通知・予定・写真を1つの画面にまとめた、自分専用のローカルダッシュボードです。複数サービスを毎回開いて確認するストレスを減らし、必要な通知だけに集中できるようにしました。通知管理だけでなく、Notionカレンダーやフォトライブラリーも組み合わせることで、外部ディスプレイに常時表示したくなる仕事用ホーム画面を目指しました。
